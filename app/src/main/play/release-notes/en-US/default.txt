Added Favorites (you can now favorite any screen to quick find it later). Added Discover (to open a random compose screen). Added New (to find only the new screens if available). Added Compose Shimmer, TreeMap and Zoomable Image examples. Updated several libraries.